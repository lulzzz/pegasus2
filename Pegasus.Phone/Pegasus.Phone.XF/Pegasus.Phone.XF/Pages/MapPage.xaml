<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="Pegasus.Phone.XF.Pages.MapPage"
             Title="Map">
  <Grid VerticalOptions="Fill" Padding="5,5,5,5">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Label Text="{Binding CraftTelemetry.Data.AltitudeSpeed, StringFormat='Craft - {0}'}"
           FontSize="Medium"
           BackgroundColor="Navy"
           XAlign="Center"
           Grid.Row="0" />
    
    <maps:Map x:Name="Map" Grid.Row="1" HeightRequest="300" HorizontalOptions="Fill"/>


    <Label Text="{Binding CraftTelemetry.Data.MainDeployStatus, StringFormat='Parachute - {0}'}"
           FontSize="Medium"
           BackgroundColor="Navy"
           XAlign="Center"
           Grid.Row="2" />
    
  </Grid>
</ContentPage>