<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:Pegasus.Phone.XF;assembly=Pegasus.Phone.XF"
             x:Class="Pegasus.Phone.XF.TextCraftView">
  <ContentView.Content>
    <StackLayout Padding="5, 5, 5, 5"
                 Orientation="Vertical"
                 Spacing="20" >

      <Label Text="Say Hello to Pegasus II Craft"
             FontSize="Medium"
             HorizontalOptions="Center" />

      <Editor HeightRequest="100" Text="{Binding Message}" />
      <Label Text="{Binding CharactersLeft,StringFormat='{0} characters left'}" TextColor="{Binding ErrorColor}" />

      <Button 
        x:Name ="SendMessageButton"
        IsEnabled="{Binding CanSubmit}"
        Text="Send"
              Command="{Binding SendCommand}"
              HorizontalOptions="Center"
              VerticalOptions="Start" />
    </StackLayout>
  </ContentView.Content>
</ContentView>
